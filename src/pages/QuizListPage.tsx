// src/pages/QuizListPage.tsx
import React from 'react';

const QuizListPage: React.FC = () => {
    return (
        <div>
            <h1>Quizzes</h1>
            <p>This page could display a list of all quizzes the current user (instructor/admin) has access to manage, possibly across different courses.</p>
            <p>Placeholder content.</p>
        </div>
    );
};

export default QuizListPage;
